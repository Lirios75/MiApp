# VEREDICTO revisor-visual — onboarding
Fecha: 2026-09-18 00:00
Screenshot: docs/revisiones/onboarding-375.png
Usabilidad: 29/40
Craft: 16/20
Copy (si vende): N-A
Fidelidad (si hubo referencia): N-A
Veredicto: NO LISTA

Top defectos:
1. [app/onboarding/page.tsx L48-68 + components/funnel/ui.tsx ConfirmSalir.onSeguir L446-449] El bug de arquitectura original (timer que pisoteaba la decisión del usuario) SÍ quedó cerrado — verificado en código: `navegacionPendienteRef` se cancela con `clearTimeout` apenas se abre `ConfirmSalir` (línea 60-63), y hay cleanup en `useEffect` al desmontar (línea 31-35). Pero la corrección abrió una PUERTA NUEVA distinta: si el usuario toca la X dentro de los 700ms tras completar el `HoldButton` y luego elige "Seguir aquí" (`onSeguir={() => setConfirmandoSalida(false)}`, línea 271), el temporizador cancelado NUNCA se reprograma y la vista `comprometido` (líneas 226-249) no tiene ningún `FunnelButton`/CTA para continuar manualmente — la pantalla de celebración queda MUERTA, sin ruta hacia /paywall salvo recargar la página o volver a abrir la X y esta vez elegir "Salir sin guardar" (perdiendo el funnel igual). Fix: al pulsar "Seguir aquí" estando `comprometido`, re-armar `navegacionPendienteRef.current = setTimeout(() => router.push('/paywall'), 700)` (o similar), o renderizar un `FunnelButton` "Continuar" visible en esa vista como respaldo permanente.
2. [components/funnel/ui.tsx ConfirmSalir mensaje por defecto, invocado sin override en app/onboarding/page.tsx L271] Cuando el modal de salida se abre DESDE el paso de compromiso, sigue mostrando el mensaje genérico "Perderás las respuestas de tu quiz" — pero `comprometerse()` (línea 49) ya ejecutó `guardarRespuestas(...)` en localStorage ANTES de que exista siquiera la posibilidad de abrir ese modal. El aviso es falso en ese momento exacto (heurística 9: mensaje engañoso sobre el estado real). Fix: pasar `mensaje="Ya guardamos tu compromiso; puedes seguir cuando quieras."` cuando `comprometido === true`, igual al patrón ya usado para diferenciar el mensaje del paywall.
3. [components/funnel/ui.tsx OptionChip L146-160, interacción con ConfirmSalir] Mismo patrón de carrera que motivó el fix de esta ronda, sin cerrar del todo: el `setTimeout(onSelect, 300)` de un chip recién tocado sigue vivo aunque el usuario abra `ConfirmSalir` (tocando la X) en esa ventana de 300ms — el modal no pausa timers de componentes por debajo. Si el timeout dispara mientras el modal está abierto, `guardarYAvanzar` avanza el `paso` en segundo plano; al cerrar el modal con "Seguir aquí" el usuario aparece ya en la siguiente pregunta sin haberlo decidido. Baja probabilidad (requiere tocar X en <300ms tras elegir opción) pero es la misma clase de bug de fondo. Fix: que `salir()` también cancele/bloquee timeouts de `OptionChip` pendientes (ej. exponiendo un flag global de "pausado" o subiendo el temporizador de selección al padre, como ya se hizo con el de compromiso).
4. [app/onboarding/page.tsx paso 6, confirmado arreglado] El centrado vertical del paso 6 pre-hold (`flex flex-1 flex-col items-center justify-center`, línea 251) ya iguala a los pasos 1 y 4 — verificado en `docs/revisiones/onboarding-paso6-375.png`, sin vacío antes del anillo de fondo. Sin acción requerida; se anota para registro.
5. [craft / eje jerarquía-profundidad, general] La pantalla usa bien los 3 niveles de profundidad (fondo mesh, card `--surface`, chip interno) pero ningún paso usa una superficie "hundida" — no es un defecto grave, es refinamiento fino: un ojo entrenado nota que la paleta de profundidad del kit (base/elevado/hundido) solo emplea 2 de los 3 niveles en estas pantallas.
